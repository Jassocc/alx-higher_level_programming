#!/usr/bin/python3
def multiply_by_2(a_dictionary):
    if a_dictionary is not None:
        new_direc = {}
        for k, v in a_dictionary.items():
            new_direc[key] = v * 2
        return new_direc
    return None
